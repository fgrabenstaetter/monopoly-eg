/*var assert = require('chai').assert;


const Offer = require('../game/offer');
const {createPlayer} = require('./factories/player');


describe('Offer', function() {
    describe('constructor()', function () {
        it('doit assigner correctement player et amount', function() {
            let player = createPlayer();

            let offer = new Offer(player, 50);
            assert.equal(offer.player, player);
            assert.equal(offer.amount, 50);
        });
    });

    describe('setAmount()', function() {
        it('doit assigner correctement amount', function() {
            let player = createPlayer();

            let offer = new Offer(player, 50);
            offer.setAmount(350);
            assert.equal(offer.amount, 350);
        });
    });
});*/
