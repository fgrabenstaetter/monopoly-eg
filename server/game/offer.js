class Offer {
     constructor (player, amount) {
         this.player = player;
         this.amount = amount;
     }

     setAmount (amount) {
         this.amount = amount;
     }
 }

 module.exports = Offer;
